<%= link_to "Back to Councillors", councillors_url %>
<br>

<h1><%= @councillor.name %></h1>

<img src="<%= @councillor.picture_url %>">

<div>
	<%= @councillor.ward %>, <%= @councillor.email %>, <%= @councillor.phone %>, <%= @councillor.website %>
</div>

<br>
<%= link_to "Edit", edit_councillor_path(@councillor) %> | <%= link_to "Delete", @councillor, method: :delete, data: {confirm: "Are you sure you want to delete this councillor?"}  %>